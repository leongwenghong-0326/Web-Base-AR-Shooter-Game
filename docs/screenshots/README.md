# Screenshots

These images are captured from this project (local XAMPP pages + real phone AR play).

| File | Source |
| --- | --- |
| `01-home.png` | Home page (`index.php`) |
| `02-game-start.png` | Game start gate (`game.php`) |
| `03-ar-play.png` | Live AR play on phone |
| `04-models.png` | Models page (`models.php`) |
| `05-compile.png` | Compile Target page (`compile-target.php`) |
| `06-target.png` | Project target image (`assets/targets/picture.jpg`) |

To refresh desktop captures later:

```bash
php tools/finalize_screenshots.php
```

Or re-run Chrome headless against `http://127.0.0.1/ar_shooter_game/` and replace the files above.
